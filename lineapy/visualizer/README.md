# Visualizer

We use `graphviz` to show the internal state of lineapy.

We use it to support demos and debugging/tests. 

The graph can be created with the tracer, which will contain more rich run-time
information, such as the variable names, and 

There are four different ways to access the visualizer currently.