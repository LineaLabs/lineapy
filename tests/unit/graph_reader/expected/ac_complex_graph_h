def get_h():
    a0 = 0
    a0 += 1
    a = 1
    a += 1
    b = a * 2 + a0
    c = b + 3
    d = a * 4
    e = d + 5
    e += 6
    a += 1
    g = c + e * 2
    h = a + g
    return h


def run_session_including_h():
    # Given multiple artifacts, we need to save each right after
    # its calculation to protect from any irrelevant downstream
    # mutations (e.g., inside other artifact calculations)
    import copy

    artifacts = dict()
    h = get_h()
    artifacts["h"] = copy.deepcopy(h)
    return artifacts


def run_all_sessions():
    artifacts = dict()
    artifacts.update(run_session_including_h())
    return artifacts


if __name__ == "__main__":
    run_all_sessions()
